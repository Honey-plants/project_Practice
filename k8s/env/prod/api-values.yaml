image:
    repository: ghcr.io/mingoo684/cicdex2-api
    tag: prod-002
    pullPolicy: IfNotPresent

imagePullSecrets:
    - name: ghcr-pull2

env:
    APP_ENV: prod
    DB_HOST: database-1.c7oeqq6c6a58.ap-northeast-2.rds.amazonaws.com
    DB_PORT: "3306"
    DB_NAME: app_db
    DB_USER: app_user
    REDIS_URL: "redis://redis:6379/0"

secretsMount:
    enabled: true
    mountPath: /run/app-secrets
    dbPasswordKey: db_password
    jwtSecretKeyKey: jwt_secret_key

secret:
    enabled: true
    dbPassword: PROD_DB_PASSWORD_CHANGE_ME
    jwtSecretKey: PROD_JWT_CHANGE_ME

ingress:
    enabled: true
    className: nginx
    host: app.example.com
    path: /api
